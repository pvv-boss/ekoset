1. Блоки с произвольным текстом для футера
2. Текстовые блоки для страниц Услуги частным лицам/Бизнесу
3. Мета-тэги для стрнаиц может брать из сущностей, а не из базы? Если из базы, то сейчас только для главной страницы беоется
4. Текстовые блоки (футера) для главной страницы
5. Клиенты текстовый блоки
6. При выводе списка новостей во вью обрезать на 3 штуки
7. При выдаче днных учитывать статус и приоритет
8. Маска телефона в дерективу (см. ниже)
9. Текстовые блоки (футера) для О компании
10. Текстовые блоки, картинка верх для О компании и Главной
11. Для разделОВ получать краткую информацию
12. Для услугОВ получать краткую информацию
13. Для индивид.предложенИЙ по виду деятельности получать краткую информацию
14. В мобиле страница Клиенты разьезжается вправо (скрол)
15. Рефакторинг мидлваре и плагиан (на один код), а также забирать наименование раздела, а не весь объект
16. Вью по услугам (для цен)...что-то было не так (у Маши спросить :)

Вопросы:
1. В футере для главной страницы какие услуги отображать
2. Для главной страницы на странице Цены что отображать
3. Клиенты зависят от раздела?
4. Где нет форм - куда должна вести ссылка заказать, вопрос...

Прочее:
1. О компании отделная страница?
2. Нет блока благодарственные письма
3. Страница штрафы?
4. Управление меню и видимостью блоков
5. Произвольная стрница из блоков
6. Соц. сети отдельный компонент?
7. Скрипты, шрифты и т.д. скачать локально
8. Настроить семантическую разметку (main, section ...)
9. Тэги для новостей
10. brc_propover - события будут чиститься? (отписка)
11. Все блоки вставить как по ТЗ




Директива для телефона
<div class="container" id="app">
  {{phone}}
  <div class="form-group">
    <div class="input-group">
      <span class="input-group-addon"><span>+7</span></span>
         <input type="tel" 
         v-model="phone"
         name="phone"
         id="phone"
         placeholder="(555) 555-5555"
         autocomplete="tel"
         maxlength="14"
         class="form-control"
         v-phone
         pattern="[(][0-9]{3}[)] [0-9]{3}-[0-9]{4}" required />
         
</div>
</div>
</div>

Vue.directive('phone', {
  bind(el) {  
    el.oninput = function(e) {
      if (!e.isTrusted) {
        return;
      }

      let x = this.value.replace(/\D/g, '').match(/(\d{0,3})(\d{0,3})(\d{0,4})/);
      this.value = !x[2] ? x[1] : '(' + x[1] + ') ' + x[2] + (x[3] ? '-' + x[3] : '');
      el.dispatchEvent(new Event('input'));
    }
  }
});

new Vue({
  el: "#app",
  data: {
    phone: ''
  }
})